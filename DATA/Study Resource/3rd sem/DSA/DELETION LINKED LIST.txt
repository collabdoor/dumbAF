1. IF START = NULL, THEN 
	WRITE UNDERFLOW AND RETURN [the list is empty]

2. IF LINK[START] = NULL, THEN [THERE IS ONLY ONE NODE IN THE LIST]
	START = NULL

3. ELSE IF LOCP = NULL, THEN [NODE TO BE DELETED IS FIRST NODE]
	START  = LINK[START]

4. ELSE
	LINK[LOCP] = LINK[LOC]
END OF STEP 2 IF

5. LINK[LOC] = AVAIL AND AVAIL = LOC